generates:
  ./src/shared/types/clear-score-aggregator-service-types.ts:
    schema: http://inves-service-appia:4002/graphql
    hooks:
      afterOneFileWrite:
        - prettier --write
    config:
      # defaultMapper: any
      skipTypename: true
      scalars:
        String: string
        Float: number
        Int: number
      declarationKind: interface
    plugins:
      - typescript
      - typescript-resolvers
